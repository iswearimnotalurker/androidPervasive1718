<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.crioprecipitati.androidpervasive1718">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity
                android:name=".viewPresenter.leader.activitySelection.ActivitySelectionActivity"
                android:label="@string/title_activity_activity_selection"
                android:theme="@style/AppTheme.NoActionBar"
                android:screenOrientation="landscape"
                >
        </activity>

        <activity
                android:name=".viewPresenter.login.LoginActivity"
                android:label="@string/title_activity_login"
                android:theme="@style/AppTheme.NoActionBar"
                android:screenOrientation="landscape"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".viewPresenter.member.taskMonitoring.TaskMonitoringActivity"
                android:label="@string/title_activity_task_monitoring"
                android:theme="@style/AppTheme.NoActionBar"
                android:screenOrientation="landscape"
                >
        </activity>

        <activity
                android:name=".viewPresenter.leader.teamMonitoring.TeamMonitoringActivity"
                android:label="@string/title_activity_team_monitoring"
                android:theme="@style/AppTheme.NoActionBar"
                android:screenOrientation="landscape"
                >
        </activity>

    </application>

</manifest>